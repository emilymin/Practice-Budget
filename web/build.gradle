apply plugin: 'idea'
apply plugin: 'war'
apply plugin: 'jetty'

repositories {
    mavenCentral()
}

dependencies{
    compile(
            'javax.servlet:servlet-api:2.5'
    )
    compile project(':core')
}

jettyRun {
    httpPort = 9000
}
